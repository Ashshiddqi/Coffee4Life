<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.coffee4life">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.Coffee4Life">

        <!-- Main Activity (Welcome Screen) -->
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:theme="@style/Theme.Coffee4Life.NoActionBar" />

        <!-- Login Activity -->
        <activity
            android:name=".LoginActivity"
            android:exported="false"
            android:theme="@style/Theme.Coffee4Life.NoActionBar"
            android:parentActivityName=".MainActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainActivity" />
        </activity>

        <!-- Sign Up Activity -->
        <activity
            android:name=".SignUpActivity"
            android:exported="false"
            android:theme="@style/Theme.Coffee4Life.NoActionBar"
            android:parentActivityName=".LoginActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".LoginActivity" />
        </activity>

        

        <!-- Profile Management Activity -->
        <activity
            android:name=".ProfileManagementActivity"
            android:exported="false"
            android:theme="@style/Theme.Coffee4Life.NoActionBar" />

        <!-- Category List Activity -->
        <activity
            android:name=".CategoryListActivity"
            android:exported="false"
            android:theme="@style/Theme.Coffee4Life.NoActionBar" />

        <!-- Product List Activity -->
        <activity
            android:name=".ProductListActivity"
            android:exported="false"
            android:theme="@style/Theme.Coffee4Life.NoActionBar" />

        

        <!-- Main Navigation Activity (single activity with bottom nav) -->
        <activity
            android:name=".MainNavActivity"
            android:exported="true"
            android:theme="@style/Theme.Coffee4Life.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>